# Item receipt template

The shift writes one file per punch-list item under `.nightshift/receipts/`. The shipped
shape is `$NIGHTSHIFT_PLUGIN_ROOT/skills/nightshift/references/templates/receipt-item.md`.
When `receipts.templatePath` is set, follow that file instead.
